package com.bassis.boot.event.listener;

import com.bassis.bean.event.ApplicationListener;
import com.bassis.boot.event.ServletEvent;

public class ServletEventListener implements ApplicationListener<ServletEvent> {

    @Override
    public void onApplicationEvent(ServletEvent event) {

    }
}
